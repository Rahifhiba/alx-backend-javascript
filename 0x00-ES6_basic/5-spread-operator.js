export default function concatArrays(array1, array2, string) {
    for (let i = 0; i < array.length; i++) {
        const element = array[i];
        
    }
    let comb = [array1, array2, string]

    return  comb
}